# Minimal dependencies for local testing (if needed)
# The vLLM worker image includes all necessary dependencies
# This file is only needed if you want to test locally with OpenAI client

# OpenAI client for testing the deployed endpoint
openai>=1.0.0

# Optional: For local testing scripts
python-dotenv>=1.0.0
